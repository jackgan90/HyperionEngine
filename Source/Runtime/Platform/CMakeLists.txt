add_library(hyperion_platform STATIC Private/Adapters/SDLWindow.cpp)
target_sources(hyperion_platform PRIVATE Private/Adapters/FileDialog.cpp)
hyp_module(hyperion_platform)
target_link_libraries(hyperion_platform PUBLIC hyperion_core PRIVATE SDL3::SDL3-static)
if(WIN32)
  target_link_libraries(hyperion_platform PRIVATE dwmapi ole32 shell32 uuid)
endif()
if(BUILD_TESTING)
  add_executable(folder_dialog_tests Private/Adapters/FileDialogTests.cpp)
  hyp_executable(folder_dialog_tests)
  target_link_libraries(folder_dialog_tests PRIVATE hyperion_platform)
  set_target_properties(folder_dialog_tests PROPERTIES FOLDER "Hyperion/Tests")
  add_test(NAME folder_dialog COMMAND folder_dialog_tests)
  set_tests_properties(folder_dialog PROPERTIES WORKING_DIRECTORY "${PROJECT_BINARY_DIR}" TIMEOUT 30 RUN_SERIAL TRUE LABELS "desktop")
endif()

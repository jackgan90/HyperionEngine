add_library(hyperion_transport STATIC Private/Transport.cpp Private/MemoryTransport.cpp)
hyp_module(hyperion_transport)
target_link_libraries(hyperion_transport PUBLIC hyperion_core)
if(WIN32)
  target_sources(hyperion_transport PRIVATE Private/Adapters/WindowsPipe.cpp Private/Adapters/WindowsPipeSecurity.cpp)
  target_link_libraries(hyperion_transport PRIVATE advapi32)
endif()
if(BUILD_TESTING)
  add_executable(transport_tests Private/TransportTests.cpp)
  hyp_executable(transport_tests)
  target_link_libraries(transport_tests PRIVATE hyperion_transport)
  add_test(NAME transport_contracts COMMAND transport_tests)
  set_tests_properties(transport_contracts PROPERTIES TIMEOUT 20)
endif()

cmake_minimum_required(VERSION 3.28)
project(Hyperion VERSION 0.1.0 LANGUAGES C CXX)
if(NOT WIN32 OR NOT MSVC OR NOT CMAKE_SIZEOF_VOID_P EQUAL 8)
  message(FATAL_ERROR "This first backend requires Windows x64 and MSVC. Other backends are not implemented yet.")
endif()
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)
set(CMAKE_CXX_SCAN_FOR_MODULES OFF)
set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>DLL")
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin")
set(CMAKE_EXPORT_COMPILE_COMMANDS ON)
include(cmake/Dependencies.cmake)
include(CTest)

function(hyp_module target)
  target_include_directories(${target} PUBLIC "${PROJECT_SOURCE_DIR}/include")
  target_compile_features(${target} PUBLIC cxx_std_20)
  target_compile_options(${target} PRIVATE /W4 /permissive- /utf-8 /EHsc)
  target_compile_definitions(${target} PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
endfunction()
function(hyp_executable target)
  hyp_module(${target})
  add_custom_command(TARGET ${target} POST_BUILD COMMAND ${CMAKE_COMMAND} -E copy_if_different $<TARGET_FILE:TBB::tbb> $<TARGET_FILE_DIR:${target}>)
  foreach(dll dxcompiler dxil)
    add_custom_command(TARGET ${target} POST_BUILD COMMAND ${CMAKE_COMMAND} -E copy_if_different "${HYP_DEPS}/dxc/bin/x64/${dll}.dll" $<TARGET_FILE_DIR:${target}>)
  endforeach()
endfunction()

add_library(hyperion_core STATIC src/adapters/Core.cpp)
hyp_module(hyperion_core)
target_link_libraries(hyperion_core PRIVATE spdlog::spdlog mimalloc-static Tracy::TracyClient)
add_library(hyperion_tasks STATIC src/adapters/TaskSystem.cpp)
hyp_module(hyperion_tasks)
target_link_libraries(hyperion_tasks PUBLIC hyperion_core PRIVATE TBB::tbb)
add_library(hyperion_config STATIC src/Config.cpp src/Plugins.cpp src/adapters/Serialization.cpp)
hyp_module(hyperion_config)
target_link_libraries(hyperion_config PUBLIC hyperion_core PRIVATE nlohmann_json::nlohmann_json)
add_library(hyperion_platform STATIC src/adapters/Platform.cpp)
hyp_module(hyperion_platform)
target_link_libraries(hyperion_platform PUBLIC hyperion_core PRIVATE SDL3::SDL3-static)
add_library(hyperion_assets STATIC src/Assets.cpp src/adapters/Math.cpp src/adapters/Mesh.cpp src/adapters/Images.cpp)
hyp_module(hyperion_assets)
target_link_libraries(hyperion_assets PUBLIC hyperion_config PRIVATE glm::glm hyp_miniz)
target_include_directories(hyperion_assets SYSTEM PRIVATE "${HYP_DEPS}/cgltf" "${HYP_DEPS}/stb" "${HYP_DEPS}/tinyexr")
add_library(hyperion_shaders STATIC src/adapters/ShaderCompiler.cpp)
hyp_module(hyperion_shaders)
target_include_directories(hyperion_shaders SYSTEM PRIVATE "${HYP_DEPS}/dxc/inc")
target_link_libraries(hyperion_shaders PUBLIC hyperion_core PRIVATE spirv-cross-msl spirv-cross-core "${HYP_DEPS}/dxc/lib/x64/dxcompiler.lib" bcrypt)
file(READ "${HYP_DEPS}/dxc/.hyperion-sha256" HYP_DXC_ID)
file(READ "${HYP_DEPS}/spirv-cross/.hyperion-sha256" HYP_CROSS_ID)
target_compile_definitions(hyperion_shaders PRIVATE HYP_TOOLCHAIN_ID="${HYP_DXC_ID}-${HYP_CROSS_ID}")
add_library(hyperion_rhi STATIC src/adapters/RHI.cpp)
hyp_module(hyperion_rhi)
target_link_libraries(hyperion_rhi PUBLIC hyperion_assets hyperion_shaders hyperion_platform PRIVATE hyp_d3d12ma d3d12 dxgi dxguid)
add_library(hyperion_render STATIC src/RenderGraph.cpp plugins/triangle/TrianglePlugin.cpp)
hyp_module(hyperion_render)
target_link_libraries(hyperion_render PUBLIC hyperion_rhi hyperion_tasks)
add_library(hyperion_gui STATIC src/adapters/Gui.cpp)
hyp_module(hyperion_gui)
target_link_libraries(hyperion_gui PUBLIC hyperion_assets hyperion_platform PRIVATE hyp_imgui hyp_implot)
add_library(hyperion_debug_ui STATIC plugins/debug_ui/DebugUIPlugin.cpp)
hyp_module(hyperion_debug_ui)
target_link_libraries(hyperion_debug_ui PUBLIC hyperion_render hyperion_gui)

add_executable(hyperion_viewer apps/viewer/Main.cpp)
hyp_executable(hyperion_viewer)
target_link_libraries(hyperion_viewer PRIVATE hyperion_debug_ui)
target_compile_definitions(hyperion_viewer PRIVATE HYP_SOURCE_DIR="${PROJECT_SOURCE_DIR}")

if(BUILD_TESTING)
  find_package(Python3 REQUIRED COMPONENTS Interpreter)
  function(hyp_test target name module source)
    add_executable(${target} "tests/${source}")
    hyp_executable(${target})
    target_link_libraries(${target} PRIVATE ${module})
    add_test(NAME ${name} COMMAND ${target})
    set_tests_properties(${name} PROPERTIES TIMEOUT 60)
  endfunction()
  hyp_test(core_tests core hyperion_core CoreTests.cpp)
  hyp_test(task_tests tasks hyperion_tasks TaskTests.cpp)
  hyp_test(config_tests configuration_plugins hyperion_config ConfigTests.cpp)
  hyp_test(asset_tests assets_math hyperion_assets AssetTests.cpp)
  hyp_test(shader_tests shaders hyperion_shaders ShaderTests.cpp)
  hyp_test(graph_tests render_graph hyperion_render GraphTests.cpp)
  hyp_test(gui_tests gui_input_and_data hyperion_gui GuiTests.cpp)
  target_compile_definitions(shader_tests PRIVATE HYP_SOURCE_DIR="${PROJECT_SOURCE_DIR}")
  add_test(NAME dependency_boundaries COMMAND "${Python3_EXECUTABLE}" "${PROJECT_SOURCE_DIR}/tools/CheckBoundaries.py")
  add_test(NAME code_style_paths COMMAND "${Python3_EXECUTABLE}" "${PROJECT_SOURCE_DIR}/tools/CheckStyle.py" --paths-only)
  add_test(NAME window_lifecycle COMMAND hyperion_viewer --frames 16 --hidden --exercise-window)
  add_test(NAME d3d12_clear COMMAND hyperion_viewer --frames 12 --hidden --capture "${PROJECT_SOURCE_DIR}/out/captures/clear.png" --verify-clear)
  add_test(NAME d3d12_triangle COMMAND hyperion_viewer --frames 16 --hidden --no-ui --capture "${PROJECT_SOURCE_DIR}/out/captures/triangle.png" --verify-triangle)
  add_test(NAME d3d12_gui COMMAND hyperion_viewer --frames 32 --hidden --capture "${PROJECT_SOURCE_DIR}/out/captures/gui.png" --verify-triangle --verify-ui)
  add_test(NAME viewer_acceptance COMMAND "${Python3_EXECUTABLE}" "${PROJECT_SOURCE_DIR}/tests/ViewerAcceptance.py" $<TARGET_FILE:hyperion_viewer> "${PROJECT_SOURCE_DIR}")
  set_tests_properties(window_lifecycle d3d12_clear d3d12_triangle d3d12_gui viewer_acceptance PROPERTIES TIMEOUT 90 LABELS "gpu;desktop" RUN_SERIAL TRUE)
  add_custom_target(hyperion_check
    COMMAND "${CMAKE_CTEST_COMMAND}" --test-dir "${PROJECT_BINARY_DIR}" -C "$<CONFIG>" --output-on-failure
    DEPENDS core_tests task_tests config_tests asset_tests shader_tests graph_tests gui_tests hyperion_viewer
    USES_TERMINAL
    COMMENT "Running Hyperion CTest suite")
endif()
if(CMAKE_GENERATOR MATCHES "^Visual Studio")
  include(cmake/VisualStudio.cmake)
endif()

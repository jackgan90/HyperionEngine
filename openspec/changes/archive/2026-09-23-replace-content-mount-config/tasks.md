## 1. Shared content service

- [x] 1.1 Extract CPU Runtime/Content with programmatic Engine setup, optional Game, prepared transitions, state generation and participant lifecycle.
- [x] 1.2 Replace asset plugin startup configuration and remove JSON mount loading.

## 2. Consumers and automation

- [x] 2.1 Route Editor transitions through shared participation and restore only preferences or explicit root arguments.
- [x] 2.2 Expose reflected root get/set/clear operations and retire automation documents safely.
- [x] 2.3 Migrate Viewer, CLI and AssetTool arguments and startup to directory-based shared APIs; preserve local tool workflows.

## 3. Migration and verification

- [x] 3.1 Migrate content scripts and integration fixtures; remove configuration files and obsolete flags.
- [x] 3.2 Cover no-root startup, root transitions, dirty/busy/stale/read-only behavior, invalid/disabled providers and lifecycle regressions.
- [x] 3.3 Update user/developer docs and architecture boundaries; validate OpenSpec and style.
- [x] 3.4 Build Debug/Release and run affected regression tests; record results without committing or archiving.

# Material system implementation evidence

The user authorized implementation after the independent plan review and explicitly excluded Git commits. Work follows tasks.md; checked tasks reflect completed implementation and relevant verification, not merely declarations. The pre-implementation audit remains in plan-review.md.

## Completed delivery (2026-09-07)

All 56 implementation and validation tasks are complete. Triangle, ModelViewer and SceneViewer use the generic material system; DebugUI and direct RHI producers use the same generic resource contracts. The accepted first-version capability limits remain as documented in `docs/Materials.md`; no out-of-scope subsystem was introduced. No Git commit or archive was made.

| Final check | Result | Evidence |
| --- | --- | --- |
| Ninja Debug full build + material GPU regression | Passed, including continuous Object retirement and bitwise World invalidation | `out/MaterialStableEvaluationBuild.log`, `out/MaterialStableEvaluationTests.log` |
| VS 2022 Debug full CTest | 42/42 passed, 104.95 s | `out/MaterialFinalVsDebug.log` |
| VS 2022 Release full CTest | 42/42 passed, 70.33 s | `out/MaterialFinalVsRelease.log` |
| Formatting / semantic naming | 225 source paths / 137 translation units passed | `out/MaterialFinalNaming.log` |
| Module boundaries | 220 module sources / 25 modules passed | `out/MaterialFinalBoundaries.log` |
| Original 514-model, 180-frame, no-culling Debug acceptance | About 22 s within unchanged 45 s threshold; 2053 draws; zero D3D12 validation errors | `out/MaterialFinalDebugSceneNone.log` |
| Change/all OpenSpec strict and final whitespace checks | Passed | `out/MaterialFinalOpenSpec.log`, `out/MaterialFinalDiffCheck.log` |

The complete suites include real material/PBR pixels, scope and descriptor sharing, Scene publication/overrides, failed Present and no-new-frame retirement, culling-mode image equality, GUI, offline startup and submitted scene/GUI RenderDoc replay assertions. HEAD remains `1847be673f72f9877e62fb2d80556e0f8a10ccc9`; source, tests, documentation and this OpenSpec change remain uncommitted in the workspace. Generated logs, screenshots, fixtures and shader caches stay under ignored `out`.

> This is a chronological implementation log. Pending statements in earlier milestones describe their state at that time and are superseded by the completion milestones below.

## CPU material contracts (tasks 1.1-1.6)

- Added independent Runtime/Materials with CPU state, typed values, owned texture/read-buffer sources, semantic registry, definitions, schema handles, Main-owned instances and immutable snapshots.
- Added Scene's direct Materials dependency and transitive data-module checks without adding rendering dependencies.
- Validation: `tools/Build.ps1 -Target material_tests` succeeded (Ninja Debug); `out/build/debug/bin/material_tests.exe` passed snapshot isolation, failed edits, type/permission checks, schema migration/stale handles, semantic precedence/clear, ambiguity, inactive values, ownership/ranges, state queries and registry conflicts.
- `python tools/CheckBoundaries.py` passed (157 source files, 25 modules); `python tools/CheckStyle.py` passed paths and formatting. Build log: `out/MaterialCpuBuild.log`.
- Renderer integration and actual GPU acceptance are pending subsequent tasks; these CPU results do not establish rendering completion.

## Shader and prepared interface (tasks 2.1-2.6)

- DXIL container reflection and SPIRV-Cross now expose member trees, target offsets/strides, logical scalar types, stage signatures, resource arrays, HLSL registers and target locations. MSL uses a preserved SPIR-V layout plus generated resource indices; MSL 2.0 is selected for texture arrays.
- Defines are normalized and versioned in cache identity. Every supported space (0-3) receives nonoverlapping shifts; DXIL logical reflection checks register ranges before SPIR-V compilation. Unstripped intermediates reconstruct metadata on cache hits.
- Renderer owns asynchronous preparation from a captured compiler/definition to an immutable Materials CPU schema and compiled program. Explicit mappings merge compatible stage bindings, conflicting overlapping stages fail, undeclared active uniforms are discovered, and inactive declarations remain writable. Cancellation/failure/retry do not mutate Main instances.
- Shader tests pass explicitly cold and hot DXIL/SPIR-V/MSL paths with nested structs, bool/int, fixed arrays, both-major non-square matrices/matrix arrays, eight textures, spaces 1/2, structured/raw buffers, changed defines and invalid space diagnostics. Interface tests pass automatic discovery, ambiguous short names, explicit cross-stage mappings, inactive values, cancellation/retry, variant type conflicts and overlapping binding rejection.
- Logs: `out/MaterialShaderBuild.log`, `out/MaterialInterfaceBuild.log`. Tests ran in `out/MaterialImplementationTests` after the sandbox could not overwrite an existing test-input file in the older build directory; a fresh output directory resolved that environment issue without permissions changes. Style and boundaries pass after integration (165 source files, 25 modules).
- DXIL legacy layout rules were checked against [Microsoft's buffer-packing documentation](https://github.com/microsoft/DirectXShaderCompiler/wiki/Buffer-Packing); no SPIR-V offsets are reused as DXIL offsets. Actual GPU packing acceptance remains task 9.7.

## Native binding foundation (tasks 3.2-3.4)

- Added D3D12 typed read buffers, independent samplers, immutable resource layouts/sets, source and visible descriptor arenas, per-stage/range/root-budget checks and recorded-packet retention. Added append-only published constant slices with exact range/publication validation and sole-owner page reset; coordinator integration remains task 3.5.
- `material_gpu_tests` passes on NVIDIA GeForce RTX 5080 with the debug layer enabled: real 0/1/8-texture pixels, independent VS texture sampling, spaces 1/2, two constant buffers, structured/raw reads, invalid/overlapping/stale constant slices, foreign sampler rejection, comparison-sampler rejection, and partial descriptor allocation rollback/recovery. Validation error count is zero.
- The first GPU fixture exposed its own t8/t9 overlap with the t3[8] texture array; layout validation correctly rejected it before recording. The fixture moved read buffers to t16/t17 and passed. Logs: `out/MaterialRhiBuild.log`, `out/MaterialGpuBuild.log`; execution working directory remains `out/MaterialImplementationTests`.
- Transitional legacy packet/pipeline fields and texture heap remain only to keep existing callers buildable until migration. Their removal and producer/fake-device updates remain unchecked tasks 3.1/3.6/8.5/8.6. This milestone does not claim the plugins have migrated.

## Graphics state, target initialization and constant packing

- Added independent complete RHI graphics state/target/dynamic types and exhaustive Materials-to-RHI and RHI-to-D3D12 conversions. Generic pipelines validate target support, shader stages/outputs/linkage, reflected resource coverage, vertex semantic/type/stride, depth/stencil compatibility and blend factors before native creation. Draw validation checks geometry index ranges, target format, slice publication and dynamic values before recording.
- Swapchain accepts D32 (default) or D32S8. Passes carry viewport, separate depth/stencil use/clear, clear values and depth domain. Graph compilation tracks color regions and independent depth/stencil initialization, invalidating depth ownership at a view-domain change. Native placed depth storage is discarded once on allocation so later partial viewport clears satisfy D3D12 initialization requirements.
- `material_gpu_tests` passed full state pixels: depth test/write, mixed depth on/off draws sharing one DSV, alpha/dynamic constant blending, channel write masks, sample mask, stencil masks/reference, a legitimate pixel-less stencil pass, front/back culling and mirrored winding. Invalid A2C/MSAA/MRT, vertex formats/strides, alpha factors, index ranges and stencil values are rejected. D3D12 debug validation count is zero. Logs: `out/MaterialStateBuild.log`.
- Added actual-reflection recursive packing of numeric/struct/array/matrix values with zero-filled padding. A GPU fixture verifies nested padded structs, struct/scalar arrays, bool/int/uint, both matrix majors, asymmetric non-square matrices and matrix arrays. Both cold and hot DXIL cache paths match an independent CPU arithmetic pixel oracle. Log: `out/MaterialPackingBuild.log`.
- Added opt-in versioned standard View/Object/Material/Scene HLSL blocks and CPU ABI schemas. Active reflected fields must match their canonical offsets/types/major order; the complete block contract fills unused stage members before merging. DXIL, SPIR-V and MSL preparation tests verify cross-stage merge, exact extents and semantic mappings, with ABI mismatch rejection. Log: `out/MaterialBlockBuild.log`.
- Style and boundary checks pass (193 owned source files; 188 checked module sources, 25 modules). Renderer frame/scope/page caching, resource publication, Scene/plugin migration and final full validation remain in subsequent unchecked tasks; state APIs do not imply caller migration.

## Constant cache foundation (task 5.4; coordinator integration pending)

- Added owned scope keys/lifetimes, versioned material context resolution and per-pass active parameter masks. Effective dependencies follow provider/default/instance/object/draw precedence; derived inputs can depend on several scopes. The complete target layout, logical/semantic mapping, exact values and scope identities/revisions participate in cache equality; hashes only choose candidate buckets.
- Added append-only constant pages, persistent/transient allocation classes, weak scope retirement and one retained idle page. Real native slices prove reuse through equal buffer payload, offset and publication token. A changed view invalidates View and mixed blocks while Object/Material reuse; distinct World values with identical caller identity/revision do not alias; a different ABI does not alias; a new material revision creates new slices. Clearing the cache while draw slices remain does not permit reset; after those references drop, collection resets and retains only one idle page.
- `material_gpu_tests` passed this additional cache fixture together with resource, state and complex packing pixels, with zero native validation errors. Build log: `out/MaterialCacheBuild.log`. These APIs are not yet wired to the session/coordinator, and actual BuildViews/plugin acceptance remains unchecked.

## Provider and native material cache foundations

- Added a frozen provider registry with explicit scope dependencies, owned CPU input snapshots, typed callback results and frequency caches. Callbacks only receive declared dependency scopes. Tests verify one Global/View evaluation across several Object inputs, exact-content invalidation even with unchanged caller revision, missing scope inputs, duplicate input rejection and frozen registration.
- Added a device-local material GPU cache for layouts, PSOs, independent texture/read-buffer sources, samplers and immutable binding sets. Structured equality follows hash selection. Independent owner groups use weak references; submitted textures remain cached through upload completion, and unused consumers retire before their source descriptors.
- Actual sampler pixels change from blue (clamp) to red (repeat) while sharing the same PSO and texture upload. Stable bindings create/copy no descriptors; sampler replacement changes only the set. Different read-buffer offsets/strides get distinct sets while sharing one buffer upload; state/target changes produce distinct PSOs. Dropping all owners and completing GPU work leaves the cache empty. `material_gpu_tests` passes with zero D3D12 validation errors; `material_binding_tests` passes provider/ABI/interface tests.
- Logs: `out/MaterialProviderBuild.log`, `out/MaterialGpuCacheBuild.log`. Last style/boundary pass before the GPU cache additions: 201 owned source files, 196 module sources/25 modules. Native cache/session/coordinator wiring remains outstanding in tasks 5.1/5.2/5.5 and 6; do not treat these isolated cache tests as Scene/plugin integration acceptance.


## Session integration and initial producer migration

- Added thread-neutral material leases with separate interface/static-resource status. The coordinator prepares shaders asynchronously, uploads static resources before any frame, shares static records across numeric revisions, and polls/collects submitted resources after failed descriptor creation. Native caches and pages are released on RHI 0 by Close, including retry after outstanding draw references.
- Added owned frame inputs, frozen session providers, view/family identities and BuildViews. All scene collections and CPU provider evaluations in a family complete in one Render task before RHI materialization. Main Viewer updates now freeze frame inputs before dispatch. Group IDs flow from the scene registry; native packets are staged by group and published completely or skipped with a frame/view/usage/revision-qualified LastDrawResult.
- Primitive Object/Draw contexts carry owned lifetimes, optional stable LocalItemId and full matrix qualifiers. Actual parameter values remain part of complete cache keys. Cache collection now distinguishes persistent cache references from outstanding draw/page references and continues without new frames until the latter retire.
- Triangle now uses a zero-texture generic material and a semantic mapping for its transform. Geometry owns vertex attributes. Model preparation uses a private PBR adapter with preserved texture mip/UV/color-space preparation and independent material snapshots. Model.hlsl consumes separate View/Object/Material/Scene blocks with the existing lighting/shading values. DebugUI has been changed to generic layout/set/constant slices; its new run is pending below.
- CPU Scene now carries model/section selection and typed override layers. FModel prepares complete owned updates before committing Main state. The bridge freezes each shared instance once, detects material-only revision changes, validates all updates, then admits new groups, existing updates and removals in one Render task. Dedicated new Scene material-selection/atomicity coverage is still pending; existing scene regression passed.
- Validation: `material_gpu_tests` passed including new zero-frame resource readiness, upload-then-descriptor failure/retirement, same-graph two-viewport pixels, exact buffer/offset sharing, five initial constant packs, zero extra packs for a stable frame, only one additional View pack after one camera changes, stable binding sets, group-wide missing-provider failure and compatible-context recovery. Latest successful run: 2026-09-07 18:24:56; log `out/MaterialSessionTestsBuild.log`.
- Triangle Viewer ran 60 frames with zero native validation errors; screenshot `out/MaterialImplementationTests/Triangle.png` was visually inspected. Log `out/MaterialTriangleRun.log`.
- `model_render_tests` passed depth/alpha/mask/sRGB/UV1/mips/mirror/double-sided pixels, gated IO/camera/recovery after migration. Its old fixed material-buffer range probes now exercise published generic CBV slices. The first run encountered an existing shader-cache write restriction; the same test passed using the authorized normal-user execution context without changing ACLs (`out/MaterialModelBuild.log`, normal-user run 18:10:06).
- `scene_render_tests` passed shared models, instance isolation, scene depth, interleaved transparency and 40 primitives (18:15:57).
- Still pending: dedicated bridge/multi-item tests; complete direct-RHI/fake producer migration and removal of legacy fields/root/heap paths; final graph capacity and failure/lifecycle coverage; current formatting/boundaries/naming and full Debug/Release CTest; final documentation and OpenSpec validation. No commit has been made.

## Producer migration, publication and retirement completed (2026-09-07)

- Removed legacy packet fields, model-only constant offsets, fixed texture arrays, alternate root-signature/recording branches, static samplers and the legacy shader-visible texture heap. Generic layouts/sets/CBV slices serve Triangle, ModelViewer/SceneViewer, DebugUI and direct RHI tests. The resource fake retains all generic dependencies and validates destruction on RHI 0; the backend-contract fake intentionally retains unsupported-default API failures. CLI/config/plugin IDs and existing executable/target names remain stable.
- Geometry and material leases are independent. Ready before the first frame does not depend on camera/Object providers; invalid section/schema overrides fail statically as soon as the interface becomes available. Context failures report qualified draw results and skip the whole group, then recover under valid inputs. Model/bridge now expose these results to both Viewer plugins separately from static readiness.
- Main bridge preparation freezes shared instances once per Flush; all preflight succeeds before the single Render admission. New attachment map nodes and receipt capacity are allocated before admission. Scene atomicity tests verify an invalid second model does not publish the valid first update or acknowledge either change. Material-only changes propagate without Scene.Update. Section/object/legacy precedence, removing subscriptions, cross-asset sharing, and delayed-old-revision result rejection pass.
- Session snapshots include logical-scene identity; custom Object/Draw provider inputs are independent from parameter-name overrides. Same-graph two-view readback demonstrates camera isolation, actual shared buffer/offset and dependency-specific packs. Multi-item tests cover same primitive/revision with different World/Draw values, reordered stable LocalItemId, changed contents and ordinal-only cross-collection isolation. Clip-space and shader-displacement conservative bounds cover pre-collection and item culling. Duplicate view/family admission is rejected.
- D3D12 immediate operations now retain submitted command lists, allocators, resources and allocations until completion, including Signal/Wait failure. A successful Idle drains all earlier uploads including unsignaled retained batches. Static material query failure is visible on material leases and clears failed cache owners once completion queries recover. Published result states release lease ownership outside their result mutex, avoiding lock inversion with RHI reporting.
- Fence-gated native tests now use a sampled material and directly observe retained constant page, binding set and sampler. They pass failed Present and recovery, removal while the queue is blocked, no early descriptor/page destruction, no-new-frame retirement, and page destruction on session close. Upload-then-visible-descriptor exhaustion and allocation rollback are also covered by material_rendering.
- Repeated geometry validation caches a bounded set of index-range min/max results on immutable native buffers, preserving direct-RHI range rejection without scanning every index on every repeated draw. The cache is locked across concurrent recording contexts.
- Documentation added: docs/Materials.md; updated SourceLayout, RenderPrimitives and AssetPipeline. Canonical HLSL block code and runtime API examples distinguish logical name, shader target and semantic; enabled capabilities and excluded work are explicit.
- Expanded targeted tests passed: `out/MaterialSessionExpanded.log`, `out/MaterialSceneExpanded.log`, `out/MaterialLifecycleExpanded.log`, `out/MaterialBoundsTests.log`. The initial scene/lifecycle run hit an older shared shader-cache write restriction; these fixtures now use isolated cache directories under the test working directory, without ACL changes. Existing resource fake tests and selected 12-test CTest regression passed in `out/MaterialCoreRegression.log`.
- Final Ninja build, full naming check and VS Debug/Release acceptance are being recorded below. No Git commit or archive has been made.

## Final scaling and cache validation

- Stable primitive item/view parameter evaluation is cached against immutable snapshot/interface, usage, effective dependency identities/revisions/full values and name overrides. Publication replaces the Render-owned cache; it is bounded to 64 combinations per primitive. Anonymous items remain collection-local. Draw keys always include frame/family/view; a stable LocalItemId never makes Draw-frequency values persistent.
- RHI prepared packet entries retain native payloads only while weak resolved-parameter/geometry/material owners are valid. Target, mirror winding, section and resources are checked on hits; dynamic state/scissor are applied every draw. Numeric-only changes preserve static resources. Index range validation caches bounded min/max data on immutable native index buffers without relaxing range or device checks.
- Schema name/alias indexes and provider scope hash buckets avoid repeated linear searches; all ambiguous names and complete cache equality remain validated. Constant collection scans once per coordinated collection and only polls orphaned pages still referenced by packets/GPU, rather than polling active cache owners. This reduced the 180-frame fixture Worker progress tasks from over 11,000 to approximately 400.
- Instrumented the original 514-model/no-culling acceptance temporarily to identify remaining hot work; removed the instrumentation afterwards. Stable queue sorting now sorts lightweight indices and moves complete material items only once. The unchanged 180-frame/2053-draw fixture completed in about 33 seconds (original timeout 45 seconds), with 514/514 models ready and zero D3D12 validation errors. Log: `out/MaterialSceneScale180Final.log`. No acceptance timeout was increased.
- RenderDoc acceptance matches the new qualified session/frame/family/view/Forward/segment scene markers while retaining submitted scene draws, GUI draws, Present and pipeline assertions.

- Final cache review found that a custom emitter could change World without publishing a primitive revision, leaving historic Object blocks associated with the primitive's long lifetime. Stable item Object tokens now change with actual World/clip-space/provider-input/name-override contents. A 64-update regression keeps the primitive/revision alive, observes at least 128 new Object packs and then verifies at most six retained blocks without another frame or primitive removal. `material_gpu_tests` passes this test (`out/MaterialObjectRetirementTests.log`), including same-graph cross-view Object sharing and earlier fence tests. `CachedBlocks` is exposed for this retirement diagnostic.

## Final validation iterations

- Source formatting and semantic C++ naming passed for 225 owned source paths and 137 translation units: `out/MaterialFinalNaming.log`. No changed function exceeds 100 lines in the final source-length review. Module checks passed for 220 module sources across 25 modules: `out/MaterialFinalBoundaries.log`.
- Ninja Debug build and the new continuous Object-retirement test passed: `out/MaterialObjectRetirementBuild.log`, `out/MaterialObjectRetirementTests.log`.
- VS 2022 Release final full CTest: **42/42 passed**, 100.54 seconds, including material/Scene pixels, all three Viewer plugins, GUI, failure/fence retirement, scene culling image equivalence, offline startup and actual RenderDoc replay acceptance. Logs: `out/MaterialFinalVsReleaseBuild.log`, `out/MaterialFinalVsRelease.log`.

- A later VS Debug full run exposed insufficient timing margin in the 514-model acceptance (41/42; the original 45-second subprocess threshold). Phase timing identified about 52 ms/frame of repeated parameter construction despite stable evaluation hits. The stable-input check now precedes derived matrix/value construction and compares complete raw Object inputs, bitwise World contents, named overrides and remaining effective scope keys/values. Frame/Pass/Draw dependencies always evaluate at their prescribed boundary. Temporary timing instrumentation was removed. The 64-update retirement test also verifies positive/negative zero World bits cause two distinct Object repacks; `out/MaterialStableEvaluationTests.log` passes.
- VS Debug incremental linking twice encountered LNK1163 COMDAT errors in generated Renderer objects. Rebuilding the specifically identified .obj files recovered linking; no source/compiler/OS policy workaround was introduced. The failed incremental log is retained in `out/MaterialVsDebugIncrementalFailure.log`. Final acceptance below supersedes earlier runs.

## Independent audit and archive handoff (2026-09-07)

- Two independent reviewers audited the Materials/Renderer and Shaders/RHI/D3D12 paths without inheriting the implementation conversation. Main independently verified findings and fixed 10 confirmed defects (one P1, nine P2); all were closed by targeted re-review. The complete findings, cache inventory, reproduction evidence and remaining capability limits are recorded in `docs/MaterialAudit.md`.
- Final VS Debug CTest: **42/42 passed**, 119.87 seconds; final VS Release CTest: **42/42 passed**, 74.97 seconds. Logs: `out/MaterialAudit/ProgramFinalVsDebug.log` and `out/MaterialAudit/ProgramFinalVsRelease.log`. These runs include actual RenderDoc replay, all Viewer plugins, native material pixels, descriptor rollback, fence/failure recovery and no-new-frame retirement.
- The original 514-model/2053-draw/180-frame no-culling fixture completed with zero native validation errors; Debug application lifetime was approximately 18.36 seconds and Release 3.45 seconds. The 45-second timeout was unchanged. Logs: `out/MaterialAudit/FinalDebugScene.log` and `FinalReleaseScene.log`.
- Final naming/format checks passed for 225 owned paths and 137 translation units; dependency boundaries passed for 220 sources and 25 modules. All 26 OpenSpec items passed strict validation, and all 56 implementation tasks are complete.
- Before archive, the 294-file final audit snapshot and audit document were rechecked without drift; the candidate contains only the reviewed material implementation and its documentation. The subsequent archive synchronizes three new capabilities and six existing capabilities. Source code is unchanged from the final tested/reviewed version; archive/spec synchronization and documentation links are the only handoff edits.

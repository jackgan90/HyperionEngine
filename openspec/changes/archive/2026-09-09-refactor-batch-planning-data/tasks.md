## 1. Baseline and workload

- [x] 1.1 Add a real-planner benchmark with controlled stable, visibility, order and value workloads plus coverage validation.
- [x] 1.2 Freeze Debug/Release benchmark baseline and document reproducible inputs alongside the frozen Viewer binaries.

## 2. Prepared planning data

- [x] 2.1 Cache immutable program/pass instance layout contracts with complete ownership validation and bounded retention.
- [x] 2.2 Prepare reusable stable item instance inputs and replace duplicated plan/chunk snapshots with shared preparations.
- [x] 2.3 Preserve shared-value partition refresh, custom strategy invalidation, ordered membership, cache budgets and retirement.

## 3. Correctness and measurements

- [x] 3.1 Add focused regressions for prepared-input reuse, shared instance overlays, changes and lifetime/cache pressure.
- [x] 3.2 Run appropriate style, naming, boundary, Debug/Release build and CTest checks.
- [x] 3.3 Compare frozen and candidate planner benchmarks and full SceneViewer stable/small/large motion with equal coverage and validation checks.
- [x] 3.4 Record implementation, measured wins/regressions and remaining bottlenecks; update OpenSpec tasks and validate artifacts.

## 4. Independent quality audit

- [x] 4.1 Fix preparation insertion rollback and separate weak planning metadata from payload byte eviction.
- [x] 4.2 Verify default Debug 2048-item reuse, small-budget ordinary fallbacks, weak-value lifetime/null handling, and allocation fault recovery.
- [x] 4.3 Recheck correctness and performance on the fixed candidate with frozen baseline comparisons.
- [x] 4.4 Complete independent targeted re-review and record dispositions, validation and remaining limits.

## 1. Reproduce and attribute

- [x] 1.1 Inspect previous optimizations and freeze the current Debug runtime with its binary hash.
- [x] 1.2 Reproduce static, small-motion and changing-visibility Debug workloads and capture CPU/GPU phase evidence.
- [x] 1.3 Split collection and draw-publication costs with permanent category-gated scopes.

## 2. Retain dependency-granular preparation

- [x] 2.1 Publish immutable shared material contracts and refresh compatible contracts once per view, with complete fallback semantics.
- [x] 2.2 Establish stable local preparation proofs and reuse batch membership and draw admission across shared-only updates.
- [x] 2.3 Retain bounded local item preparation across visibility transitions and remove measured repeated collection work.
- [x] 2.4 Preserve current receipts, failure handling, immutable old graphs and resource retirement, and expose useful operation counts.

## 3. Validate and measure

- [x] 3.1 Add and run targeted semantic, visibility, ownership, cache-bound and invalidation regressions.
- [x] 3.2 Complete Debug/Release builds and appropriate CTest, style, naming, boundary and OpenSpec checks.
- [x] 3.3 Run alternating frozen-baseline Debug/Release measurements and exact scene/image coverage comparisons, plus sustained motion and GUI rendering/readback verification.
- [x] 3.4 Document the confirmed cause, final architecture, all measured outcomes and remaining limitations.

## 4. Independent quality audit

- [x] 4.1 Independently review the frozen working changes and verify each reported finding against source and raw evidence.
- [x] 4.2 Guard the public collection boundary against obsolete/foreign snapshots, add direct API regressions, and correct the image/configuration evidence descriptions.
- [x] 4.3 Build both configurations, run relevant regressions and checks, and record bounded post-fix performance/image comparisons separately from initial delivery evidence; reproduce and fix the two existing test precondition gaps without changing engine retirement semantics.
- [x] 4.4 Obtain targeted re-review from an independent reviewer and report remaining limitations.

## 1. Baseline and test contracts

- [x] 1.1 Freeze current Debug/Release runtime identity and retain the investigation's visibility and frame measurements.
- [x] 1.2 Add executable regressions for unchanged visible membership, first/middle/last batch changes, shared compatibility changes and retained-frame ownership.

## 2. Visible membership and material preparation

- [x] 2.1 Add exact prepared visibility selection for eligible static sources with unchanged-member storage reuse and the complete collection fallback.
- [x] 2.2 Retain conservative view-independent bounds information and skip redundant fine tests only with a valid containment proof.
- [x] 2.3 Retain shared binding groups for unchanged membership, refresh current shared values and preserve full material fallback.

## 3. Incremental batch planning and submission

- [x] 3.1 Add bounded persistent source/group/block metadata and complete eligibility, identity and shared-value checks.
- [x] 3.2 Apply membership/local-input changes to affected blocks, preserve independent block identities/data and publish immutable plans with full fallback.
- [x] 3.3 Reuse batch-level draw admission across unrelated membership changes while preserving current bindings, receipts and grouped failure repair.
- [x] 3.4 Preserve explicit source invalidation, cache capacity and old-frame ownership; reduce measured redundant CPU cache maintenance within those constraints.

## 4. Diagnostics and verification

- [x] 4.1 Add membership delta, incremental plan, affected block and batch admission diagnostics to Viewer/benchmark output.
- [x] 4.2 Cover transparent/custom/unsupported/invalid-identity fallback, shared/resource changes, capacity churn, removal and independently retained frames.
- [x] 4.3 Validate Debug and Release builds and relevant complete test suites, style, semantic naming, boundaries and whitespace.
- [x] 4.4 Run serial frozen-baseline A/B for static/small/large motion plus fixed-eye rotation and local-change probes; verify ready coverage, images, GPU identities and native validation.
- [x] 4.5 Record final implementation, raw evidence, remaining costs/regressions and strict OpenSpec validation; leave all changes uncommitted.

## 5. Independent audit repair

- [x] 5.1 Remove persistent compiled/default source ownership from incremental groups and regress stopped views, source removal and retained old graphs.
- [x] 5.2 Preserve CapacitySplits for incremental initial layouts, membership changes and reuse.
- [x] 5.3 Validate the repairs, obtain independent re-review and record final evidence without committing or archiving.

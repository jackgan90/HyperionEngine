## 1. 输入与解析

- [x] 1.1 保存基线 trace、构建配置和 Debug/Release 数据，确认 camera motion 热点。
- [x] 1.2 消除无变化 setter 的 snapshot 复制，并共享经过验证的不可变 scope 输入。
- [x] 1.3 实现通用有效依赖增量解析，共享未变参数并保留 override/default、混合 scope 与资源身份语义。

## 2. 常量与缓存

- [x] 2.1 按反射常量块复用未变 slice，避免每个 draw 重做相同共享块的完整匹配。
- [x] 2.2 为参数/provider/常量历史明确容量和退休规则，增加可验证的缓存统计。
- [x] 2.3 压测同 key 不同值、预算淘汰、旧帧保活与空闲清理，调整并记录默认预算和页面策略。

## 3. 原生命令

- [x] 3.1 增加 D3D12 单次命令列表录制内的 root signature、heap、CBV 与 descriptor table 状态复用。
- [x] 3.2 验证切换、重复录制、失败恢复与逐 draw 校验不受影响。

## 4. 验证与交付

- [x] 4.1 补充必要的多 scope、共享/混合块、无变化和缓存生命周期回归测试及操作计数。
- [x] 4.2 完成 Debug/Release 构建、相关 CTest、格式、命名与模块边界检查。
- [x] 4.3 用相同场景和配置采集优化后静止/运动重复样本及长时样本，形成前后对比。
- [x] 4.4 按测量处理剩余热点，将最终实现决策、证据、限制同步到 OpenSpec artifacts 并验证。

## 5. 独立审计修复

- [x] 5.1 修复跨 primitive Draw scope 身份碰撞，并覆盖稳定、不同和缺失 LocalItemId。
- [x] 5.2 修复 64 项边界的循环扫描抖动，验证 64/65/128 项、增量刷新和工作集更换。
- [x] 5.3 用访问顺序索引消除 provider/常量预算逐出的全表扫描，验证命中、替换、清理和容量压力。
- [x] 5.4 修复关闭后的实时 PageBytes，完成相关检查、独立复审并同步审计证据与后续可选方向。

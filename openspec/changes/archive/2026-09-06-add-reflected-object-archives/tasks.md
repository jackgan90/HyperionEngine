## 1. Implementation and verification

- [x] 1.1 Implement typed record/member registration, nested values, enums and binary arithmetic arrays.
- [x] 1.2 Implement versioned bounded binary archive encoding/decoding with transactional construction.
- [x] 1.3 Expose legacy reflected JSON memory encode/decode without changing persisted keys.
- [x] 1.4 Test nested round trips, defaults, unknown fields, malformed/truncated input and failed transactional reads; record build evidence.
